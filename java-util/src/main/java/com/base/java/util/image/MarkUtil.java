package com.base.java.util.image;

import java.nio.ByteBuffer;

/**
 * 添加图片水印
 */
public class MarkUtil {
    public static ByteBuffer markImageByIcon(String conPath, ByteBuffer imageBuffer, Integer degree) {
//        ByteBuffer outBuffer = null;
//        try {
//            Image licenseImage = ImageIO.read(new ByteArrayInputStream(imageBuffer.array()));
//
//            BufferedImage bufferedImage = new BufferedImage(licenseImage.getWidth(null));
//            licenseImage.getHeight(null), bufferedImage;
//
//            Graphics2D graphics = bufferedImage.createGraphics();
//            graphics.setRender();
//
//        }
        return null;
    }
}
